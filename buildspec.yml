version: 0.2

phases:
  build:
    commands:
      - aws deploy push --application-name cicd-handson-app --s3-location s3://wark5/artifact.zip --source src
artifacts:
  files:
    - '**/*'
  base-directory: src